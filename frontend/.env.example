# Backend URL — only needed for local dev.
# In production the React app is served by the same FastAPI process,
# so it auto-detects the origin and no value is needed here.
VITE_API_URL=http://localhost:8000
